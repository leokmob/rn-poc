h1= title
h5 Enter a phone number (+1<10 digit number>) and a text message and click Send SMS. You will receive the message that will have a short url to your message accompanied by music.
br
br
form(method="post", action="/sendSMS")
    div
        label(for="toText") To:
        input(type="text", name="toText", id="toText", value=to, size="12")
    br
    div
        label(for="textText") Text:
        input(type="text", name="textText", id="textText", value=text, size="40")
    br
    div
        input(type="submit", value="Send SMS")
